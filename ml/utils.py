# utils.py: Helper functions for ML
import numpy as np
import sqlite3
from typing import List, Dict, Any, Optional
import logging

# Configure dev logging
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)

# Preprocesses text for embeddings
#class TextPreprocessor:

# Validates data (no missing values, empty fields)
#class DataValidator:

# Handles model db connections and queries
#class DatabaseConnector:

# Handles generating text embeddings
#class EmbeddingGenerator:

# Manages storage/retrieval of vectors + cosine sim calculations
#class VectorStore:
